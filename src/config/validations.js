export const VALIDATIONS = {
    UPSTRING: /[^a-zA-Z]/g,
    ALPHANUM: /[^a-zA-Z0-9]/g,
    DIGIT: /[^0-9]/g,
    EMAIL: /[^0-9a-zA-Z@._-]/g,
    ALPHANUMWITHSPACE: /[^a-zA-Z0-9 ]/g,
    UPI: /[^a-zA-Z0-9@]/g
  };
  